apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-message-one.fullname" . }}
  labels:
    {{- include "app-message-one.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-message-one.selectorLabels" . | nindent 4 }}
