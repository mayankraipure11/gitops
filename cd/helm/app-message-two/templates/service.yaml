apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-message-two.fullname" . }}
  labels:
    {{- include "app-message-two.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-message-two.selectorLabels" . | nindent 4 }}
