apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-message-two.fullname" . }}-test-connection"
  labels:
    {{- include "app-message-two.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-message-two.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
