apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-message-two.fullname" . }}-configmap
data:
  baseUrl: {{ .Values.cmcfg.baseUrl | quote }}
  # baseUrl: "app-message-app-message-one"
  port: "8081"
  protocol: "http"