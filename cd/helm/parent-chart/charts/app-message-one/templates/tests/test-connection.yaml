apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-message-one.fullname" . }}-test-connection"
  labels:
    {{- include "app-message-one.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-message-one.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
